rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 用户资料 - 用户只能读写自己的资料
    match /users/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }

    // 预约 - 用户创建和管理自己的预约
    match /appointments/{appointmentId} {
      allow read, write: if request.auth != null && 
        (request.auth.uid == resource.data.userId || 
         request.auth.uid == resource.data.nutritionistId);
    }

    // 评价 - 公开读取，认证用户可写
    match /ratings/{ratingId} {
      allow read: if true;
      allow write: if request.auth != null;
    }

    // 公开数据可读，认证用户可写
    match /resources/{resourceId} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}


rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 用户资料 - 用户只能读写自己的资料
    match /users/{userId} {
      allow read, write: if request.auth != null;
    }

    // 预约 - 用户创建和管理自己的预约
    match /appointments/{appointmentId} {
      allow read, write: if request.auth != null;
    }

    // 评价 - 公开读取，认证用户可写
    match /ratings/{ratingId} {
      allow read: if true;
      allow write: if request.auth != null;
    }

    // 公开数据可读，认证用户可写
    match /resources/{resourceId} {
      allow read: if true;
      allow write: if request.auth != null;
    }

    match /blogPosts/{blogPostId} {
      allow read: if true;
      allow write: if request.auth != null;
    }

    match /bulkEmailLogs/{bulkEmailLogId} {
      allow read: if true;
      allow write: if request.auth != null;
    }

    match /emailLogs/{emailLogId} {
      allow read: if true;
      allow write: if request.auth != null;
    }

    match /nutritionLogs/{nutritionLogId} {
      allow read: if true;
      allow write: if request.auth != null;
    }

    match /postComments/{postCommentId} {
      allow read: if true;
      allow write: if request.auth != null;
    }

    match /postLikes/{postLikeId} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}